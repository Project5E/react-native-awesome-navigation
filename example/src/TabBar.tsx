import React from 'react'
import {View, Button} from 'react-native'

const TabBar = () => {
  return (
    <View style={{backgroundColor: 'yellow', flex: 1, flexDirection: 'row', justifyContent: ''}}>
      <Button
        onPress={() => {
          //
        }}
        title="1111"
      />
      <Button
        onPress={() => {
          //
        }}
        title="2222"
      />
    </View>
  )
}

export default TabBar
